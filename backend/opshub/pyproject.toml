[project]
name = "opshub"
version = "1.0.0"
description = "Fleet control plane: logs + health + metrics + dashboard + launcher"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.32",
  "aiosqlite>=0.20",
  "docker>=7.1",
  "apscheduler>=3.10,<4",
  "bcrypt>=4.2",
  "httpx>=0.27",
]

[tool.uv]
package = false
