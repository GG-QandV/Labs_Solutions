[project]
name = "agents-demo"
version = "0.1.0"
description = "Cross-document check demo: two independent agents + arbiter"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "httpx>=0.27",
]

[tool.uv]
package = false
